import type { PortfolioTranslations } from "@/locales/types";
import en from "@/locales/en.json";

/** @deprecated Use useLocale() and translations from LocaleProvider instead. Kept for type compatibility. */
export const portfolioData: PortfolioTranslations = en as PortfolioTranslations;
